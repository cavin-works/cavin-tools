# Cavin Tools - AI 辅助开发指南

> 本文档记录项目的重大变更、技术决策和 AI Agent 协作信息
> 最后更新: 2026-01-29

---

## 项目激活状态

✅ **Serena 已激活** - 项目使用 Serena 记忆系统管理代码上下文

---

## 📋 项目概览

### 基本信息
- **项目名称**: Cavin Tools
- **类型**: Tauri 桌面应用
- **当前版本**: 0.0.8
- **发布策略**: 自动化语义化版本 (semantic-release)

### 核心技术栈
```
前端: React 18.3.1 + TypeScript 5.7.2 + Vite 6.0.3
样式: TailwindCSS 4.x + shadcn/ui
状态: Zustand 5.0.2
后端: Tauri 2.x + Rust + FFmpeg
package: pnpm
```

---

## 🏗️ 架构决策记录

### 1. 为什么选择 Tauri 而不是 Electron？
- **更小的安装包**: Tauri 使用系统 WebView，应用体积 < 10MB
- **更好的性能**: Rust 后端内存占用更低
- **更安全的架构**: 能力安全模型，精细权限控制

### 2. 为什么使用 Zustand 而不是 Redux？
- **更简洁的 API**: 无需 actions/reducers 样板代码
- **更好的 TypeScript 支持**: 类型推导更简单
- **更小的包体积**: 约 1KB vs Redux 的 5KB+

### 3. 为什么选择 TailwindCSS 4.x？
- **CSS-first 配置**: 无需 JavaScript 配置文件
- **更好的性能**: 原生 CSS 引擎，无运行时开销
- **主题系统**: 通过 CSS 变量实现 5 种主题 + 亮/暗模式

### 4. 为什么使用 shadcn/ui？
- **组件所有权**: 代码直接在项目中，完全可定制
- **基于 Radix UI**: 无障碍访问友好
- **与 Tailwind 完美集成**: 样式一致性

---

## 📁 项目结构约定

### 标准工具目录结构
```
src/tools/{category}/{tool}/
├── components/           # 工具专用组件
├── store/                # Zustand 状态管理
├── types/                # TypeScript 类型
├── utils/                # 工具函数
├── tool.config.ts        # 工具配置（必须）
└── index.tsx             # 工具入口组件
```

### 核心目录
- `src/core/tool-registry/` - 工具注册系统（新工具必须在此注册）
- `src/core/theme/` - 主题配置（5种主题 + 亮/暗模式）
- `src/shared/components/ui/` - shadcn/ui 组件库

---

## 🔧 开发规范

### 必须遵守的规则
1. **禁止 `any` 类型** - 使用 `unknown` 或具体类型
2. **禁止 `@ts-ignore`** - 修复类型错误而不是忽略
3. **必须类型标注** - 所有变量、函数参数显式标注类型
4. **必须使用工具注册** - 新工具在 `toolRegistry.ts` 注册

### 提交规范 (Conventional Commits)
```
feat(video-editor): 添加视频截断功能
fix(image-converter): 修复转换后图片损坏
docs: 更新 README 开发文档
refactor(core): 重构工具注册逻辑
chore(deps): 升级 React 到 18.3.1
```

### 任务完成检查清单
- [ ] TypeScript 类型检查通过 (`npx tsc --noEmit`)
- [ ] 构建成功 (`npm run build`)
- [ ] 移除所有调试代码
- [ ] 新工具已注册到 toolRegistry
- [ ] 提交信息符合 commitlint 规范

---

## 🚀 常用命令

### 开发
```bash
npm run tauri dev     # 启动完整开发环境
npm run dev          # 仅前端开发服务器
```

### 构建与测试
```bash
npm run build        # TypeScript 检查 + 构建
npm test             # 运行测试
npm run tauri build  # 构建生产版本
```

### 版本管理
```bash
npm run version:sync # 同步版本号（package/Cargo/tauri.conf）
```

---

## 📦 当前工具列表

| 分类 | 工具 | 状态 |
|------|------|------|
| 🎬 视频 | 视频编辑器 | ✅ 稳定 |
| 🖼️ 图像 | 图像转换器 | ✅ 稳定 |
| 🖼️ 图像 | 图像压缩器 | ✅ 稳定 |
| 🖼️ 图像 | 水印去除 | ✅ 稳定 |
| 🖼️ 图像 | 背景去除 | ✅ 稳定 |
| 🔧 开发 | 进程管理器 | ✅ 稳定 |
| 📝 文本 | 字符工具 | ✅ 稳定 |

---

## 🎯 重大变更记录

### 2026-01-29: Serena 记忆系统集成
- **决策**: 激活 Serena 记忆项目，管理代码上下文
- **影响**: AI Agent 可通过 `.serena/memories/` 访问项目知识
- **配置**: 项目名 `cavin-tools`，语言服务器 TypeScript

### 2025: 依赖版本升级
- **变更**: `@tauri-apps/api` 从 2.8.0 升级到 2.9.1
- **原因**: 匹配 Rust crate 版本，修复类型不匹配
- **影响**: 需要 `cargo check` 确保兼容性

### 2025: TailwindCSS 4.x 迁移
- **决策**: 从 TailwindCSS 3.x 升级到 4.x
- **变更**: CSS-first 配置，移除 `tailwind.config.js`
- **影响**: 主题系统通过 CSS 变量实现

### 持续: semantic-release 自动化
- **流程**: Git 提交 → 自动生成 changelog → 自动发布版本
- **要求**: 提交信息必须符合 conventional commits 规范
- **配置**: `.releaserc.js` + `commitlint.config.js`

---

## 🔮 未来规划

### 待实现功能
- [ ] 图片编辑器 - 标注、裁剪、滤镜
- [ ] 图片拼贴 - 多图拼接、模板
- [ ] 批量处理 - 批量转换/压缩

### 技术债务
- [ ] 添加单元测试覆盖率
- [ ] 添加 E2E 测试
- [ ] 性能优化（视频编码、大图片处理）

---

## 📚 Serena 记忆文件

项目使用 Serena 记忆系统存储以下知识：

| 记忆文件 | 内容 |
|----------|------|
| `project_overview.md` | 项目概述、工具列表、技术栈 |
| `project_structure.md` | 完整目录结构说明 |
| `code_style_conventions.md` | 代码风格、命名规范、状态管理模式 |
| `suggested_commands.md` | 开发、测试、构建命令 |
| `completion_guidelines.md` | 任务完成检查清单 |

**注意**: AI Agent 在执行任务前应先使用 `activate_project` 激活 `cavin-tools` 项目

---

## 🤖 AI Agent 协作指南

### 开始新任务前
1. **激活项目**: 使用 Serena `activate_project` 工具
2. **读取记忆**: 使用 `read_memory` 加载相关上下文
3. **理解架构**: 查阅项目结构和代码规范

### 实现任务时
1. **创建 TODO**: 使用 `todowrite` 规划任务步骤
2. **遵循规范**: 按照项目结构创建文件
3. **注册工具**: 新功能必须在 `toolRegistry.ts` 注册

### 完成任务后
1. **类型检查**: `npx tsc --noEmit`
2. **构建验证**: `npm run build`
3. **更新记忆**: 如有重大变更，使用 `write_memory` 更新 Serena

---

## 📞 联系与维护

- **项目路径**: `/Users/cavinhuang/workspace/projects/ai-projects/cavin-tools`
- **Serena 配置**: `.serena/project.yml`
- **主要维护者**: Cavin Huang

---

*本文档应随着项目演进持续更新*
